# Requirements for RealSense T265 + YOLO Motion Detection Project
# For MacBook M4 (Apple Silicon)

# Core Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0

# YOLO Detection
ultralytics>=8.0.0

# RealSense (Note: pyrealsense2 may need manual installation on macOS)
# pip install pyrealsense2  # Or build from source

# Optional: Depth Anything V2 (for depth verification)
# pip install git+https://github.com/DepthAnything/Depth-Anything-V2.git

# Tracking
filterpy>=1.4.5  # For Kalman Filter

# Utilities
pyyaml>=6.0
tqdm>=4.65.0

# Note for macOS M4:
# 1. Install librealsense first: brew install librealsense
# 2. Some packages may need Rosetta 2 or ARM-native builds
# 3. Use MPS acceleration: device='mps' in PyTorch
