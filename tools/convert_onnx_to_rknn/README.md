# ONNX to RKNN Conversion

This directory is reserved for the future `ONNX -> RKNN` model conversion toolchain.

Expected future contents:
- version-locked conversion script
- calibration dataset notes
- preprocessing and postprocessing parity checks against the Windows ONNX path
